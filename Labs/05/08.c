#include <stdio.h>
int main(){
    int item, b, c;
    printf("Enter the value of each item");
    scanf("%d", &item);
    
    printf("Enter the value of ");
    // scanf("");
    
    if(item < 2000){
        item = item (item * 0.5);
        printf("");
    }
}
